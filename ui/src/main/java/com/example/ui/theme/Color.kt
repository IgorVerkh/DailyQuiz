package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Main app background - used for main page and question panes background
val MainBackground = Color(0xFF7067FF)

// App pane background - used for question panels, cards, etc.
val PaneBackground = Color(0xFFFFFFFF)

// Light gray background - used for idle answer options
val LightGrayBackground = Color(0xFFF3F3F3)

// Element background - used for disabled buttons
val ElementBackground = Color(0xFFBABABA)

// Accent color - used for small areas like timer widget, selected answer text
val AccentColor = Color(0xFF2B0063)

// Secondary text color - used for question numbers, secondary information
val SecondaryText = Color(0xFFBCB7FF)

// Success color - used for correct answers
val SuccessColor = Color(0xFF00AE3A)

// Error color - used for incorrect answers
val ErrorColor = Color(0xFFE70000) 